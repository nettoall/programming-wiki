<div id="page">

<div id="main" class="aui-page-panel">

<div id="main-header">

<div id="breadcrumb-section">

1.  [Programming](README.md)
2.  [Programming](Programming_98307.md)
3.  [Java](Java_25001989.md)
4.  [Java Basic](Java-Basic_399278081.md)
5.  [Thread](Thread_40042767.md)

</div>

# <span id="title-text"> Programming : Thread 상태제어 </span>

</div>

<div id="content" class="view">

<div class="page-metadata">

Created by <span class="author"> Dongwook Han</span>, last modified on 6월 16, 2023

</div>

<div id="main-content" class="wiki-content group">

# <style>[data-colorid=g4oywpt5vp]{color:#ff5630} html[data-color-mode=dark] [data-colorid=g4oywpt5vp]{color:#cf2600}[data-colorid=u26g64di3r]{color:#ff5630} html[data-color-mode=dark] [data-colorid=u26g64di3r]{color:#cf2600}[data-colorid=gjlxc7fo73]{color:#ff5630} html[data-color-mode=dark] [data-colorid=gjlxc7fo73]{color:#cf2600}[data-colorid=daia1nywxs]{color:#ff5630} html[data-color-mode=dark] [data-colorid=daia1nywxs]{color:#cf2600}</style>Thread와 상태 제어

<div id="ap-com.mxgraph.confluence.plugins.diagramly__drawio71795194327009466" class="ap-container">

<div id="embedded-com.mxgraph.confluence.plugins.diagramly__drawio71795194327009466" class="ap-content">

</div>

</div>

- Thread 객체를 생성하고 start() 메소드를 호출하면 실행 대기 상태(Runnable) 에 들어감

- 실행 대기 상태에서 스레드 스케줄링에 의해 선택된 스레드가 실행(Running)

- 실행중인 상태에서 일시 정지(WAITING, TIMED_WAITING, BLOCKED) 상태로 이동

## Thread 상태 확인

- 스레드 상태 출력 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  public class StatePrintThread extends Thread {
    private Thread targetThread;
    
    public StatePrintThread(Thread targetThread) {
      this.targetThread = targetThread;
    }
    
    public void run(){
      while(treu) {
        Thread.State state = targetThread.getState();
        System.out.println("타겟 스레드 상태 : " + state);
        
        if(state == Thread.State.NEW){
          targetThread.start();
        }
        
        if(state == Thread.State.TERMINATED){
          break;
        }
        
        try{
          // 0.5초간 일시 정지
          Thread.sleep(500);
        }catch(Exception e){}
      }
    }
  }

  public class TargetThread extends Thread {
    public void run(){
      for(long i = 0; i < 1000000000; i++){}
      
      try {
        //1.5초간 일시 정지
        Thread.sleep(1500);
      }catch(Exception e){
      }
      
      for(long i = 0; i < 1000000000; i++){}
    }
  }

  public class ThreadStateExample{
    public static void main(String[] args){
      StatePrintThread statePrintThread = new StatePrintThread(new TargetThread());
      statePrintThread.start();
    }
  }
  ```

  </div>

  </div>

## Java Thread 상태제어 API(review 시 page 600)

<div class="table-wrap">

<table class="confluenceTable" data-layout="default" data-local-id="870c3d52-b852-4cad-9386-bd64fe424dfe">
<tbody>
<tr>
<th class="confluenceTh"><p><strong>메소드</strong></p></th>
<th class="confluenceTh"><p><strong>설명</strong></p></th>
</tr>
&#10;<tr>
<td class="confluenceTd"><p>interrupt()</p></td>
<td class="confluenceTd"><p>일시 정지 상태의 스레드에서 InterruptedException 에뢰를 발생시켜, 에외 처리코드(catch)에서 실행 대기 상태로 가거나 종료 상태로 갈 수 있도록 한다.</p></td>
</tr>
<tr>
<td class="confluenceTd"><p>notify()</p>
<p>notifyAll()</p></td>
<td class="confluenceTd"><p>동기화 블록 내에서 wait() 메소드에 의해 일시 정지 상태에 있는 스레드를 실행 대기 상태로 만든다.</p></td>
</tr>
<tr>
<td class="confluenceTd"><p><del>resume()</del></p></td>
<td class="confluenceTd"><p>suspennd() 메소드에 의해 일시 정지 상태에 있는 스레드를 실행 대기 상태로 만든다. - <strong><span data-colorid="daia1nywxs">Deprecard</span></strong>(대신 notify(), notifyAll() 사용)</p></td>
</tr>
<tr>
<td class="confluenceTd"><p>sleep(long millis)</p>
<p>sleep(long millis, int nanos)</p></td>
<td class="confluenceTd"><p>주어진 시간 동안 스레드를 일시 정지 상태로 만든다. 주어진 시간이 지나면 자동적으로 실행 대기 상태로 된다.</p></td>
</tr>
<tr>
<td class="confluenceTd"><p>join()</p>
<p>join(long millis)</p>
<p>join(long millis, int nanos)</p></td>
<td class="confluenceTd"><p>join() 메소드를 호출한 스레드는 일시 정지 상태가 된다. 실행 대기 상태로 가려면, join() 메소드를 멤버로 가지는 스레드가 종료되거나, 매개값으로 주어진 시간이 지나야 한다.</p></td>
</tr>
<tr>
<td class="confluenceTd"><p>wait()</p>
<p>wait(long millis)</p>
<p>wait(long millis, int nanos)</p></td>
<td class="confluenceTd"><p>동기화(synchronized0 블록 내에서 스레드를 일시 정지 사앹로 만든다. 매개값으로 주어진 시간이 지나면 자동적으로 실행 대기 상태가 된다. 시간이 주어지지 않으면 notify(), notifyAll() 메소드에 의해 실행 대기 상태로 갈 수 있다.</p></td>
</tr>
<tr>
<td class="confluenceTd"><p><del>suspend()</del></p></td>
<td class="confluenceTd"><p>스레드를 일시 정지 상태로 만든다. resume() 메소드를 호ㅜㄹ하면 다시 실행 대기 상태가 된다. -<strong><span data-colorid="gjlxc7fo73"> Deprecated</span></strong>(대신 wait() 사용)</p></td>
</tr>
<tr>
<td class="confluenceTd"><p>yield()</p></td>
<td class="confluenceTd"><p>실행 중에 우선 순위가 동일한 다른 스레드에게 실행을 양보하고 실행 대기 상태가 된다.</p></td>
</tr>
<tr>
<td class="confluenceTd"><p><del>stop()</del></p></td>
<td class="confluenceTd"><p>스레드를 즉시 종료시킨다. -<span data-colorid="u26g64di3r"> </span><strong><span data-colorid="g4oywpt5vp">Deprecated</span></strong></p></td>
</tr>
</tbody>
</table>

</div>

#### sleep

- 실행 중인 스레드를 일정 시간 멈춤

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  try{
    Thread.sleep(1000);
  }catch(InterruptedException e){
    //interrupt() 메소드가 호출되면 실행
  }
  ```

  </div>

  </div>

#### join

- Thread가 종료될 때까지 대기

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  ThreadExam(){
    Thread1 thread1 = new Thread1();
    thread1.start();
    thread1.join(); // try catch interrupted, thread1 종료까지 main 대기
  }
  ```

  </div>

  </div>

#### wait, notify

- 스레드간 협업

- 동기화된 블록 내에서만 사용

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  public class ThreadB extends Thread{
    int total;
    
    @Override
    public void run(){
      synchronized(this){
        for(int i = 0; i < 5; i++){
          ...
        }
        notify(); // thread 작업 종료 후 다른 thread 에게 notify
      }
    }
  }

  public class ThreadA extends Thread{
    public static void main(String[] args){
      ThreadB b = new ThreadB();
      b.start();
      
      synchronized(b){
        try{
          System.out.println("b가 완료될때까지 기다립니다.");
          b.wait();
        }catch(InterruptedException e){
          e.printStackTrace();
        }
      }
    }
  }
  ```

  </div>

  </div>

#### yield

- 다른 스레드에게 실행을 양보하고 자신을 실행 대기 상태로 가도록 실행

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  public class YieldExample{
    public static void main(String[] args){
      ThreadA threadA = new ThreadA();
      ThreadB threadB = new ThreadB();
      
      threadA.start();
      thradB.satrt();
      
      try{
        Thread.sleep(3000);
      }catch(InterruptedException e){}
      thradA.work = false;
      
      try{
        Thread.sleep(3000);
      }catch(InterruptedException e){}
      threadA.work = true;
      
      try{
        Thread.sleep(3000);
      }catch(InterruptedException e){}
      threadA.stop = true;
      threadB.stop = true;
    }
  }

  public class ThreadA extends Thread{
    public boolean stop = false; // 종료 플래스
    public boolean work = true;  // 작업 진행 여부 플래그
    
    public void run(){
      while(!stop){
        if(work){
          System.out.println("ThreadA 작업 내용");
        }else{
          Thread.yield();
        }
      }
      System.out.println("ThreadA 종료");
    }
  }

  public class ThreadB extends Thread{
    public boolean stop = false;
    public boolean work = true;
    
    public void run(){
      while(!stop){
        if(work){
          System.out.println("ThreadB 작업 내용");
        }else{
          Thread.yield();
        }
      }
      System.out.println("ThreadB 종료");
    }
  }
  ```

  </div>

  </div>

#### interrupt

- 스레드가 일시 정지 상태에 있을 때 InterruptedException 예외를 발생

- InterruptedException 발생시 run() 메소드 정상 종료

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  public class InterruptExample{
    public static void main(String[] args){
      Thread thread = new PrintThread2();
      thread.start();
      try{
        Thread.sleep(1000);
      }catch(InterruptedException e){
      
      }
      thread.interrupt();  // 스레드 종료시키기 위해 InterruptedExceptoin 발생
    }
  }

  public class PrintThread2 extends Thread{
    public void run(){
      try{
        while(true){
          System.out.println("실행중");
          Thread.sleep(1);
        }
      }catch(InterruptedException e){
      }
      System.out.println("자원 정리");
      System.out.println("실행 종료");
    }
  }
  ```

  </div>

  </div>

# Daemon Thread

- 백그라운드에서 작업 실행

- Main Thread가 종료되면 강제 종료됨

- start() 메소드 호출전에 setDeamon(true) 호출 해야 함

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  public class ThreadA implements Runnable{
    @Override
    public void run(){
      while(true){
        ...
      }
    }
    
    public static void main(String[] args){
      Thread th = new Thread(new ThreadA);
      th.setDaemon(true); //daemon 설정
      th.start();
    }//Main Thread 종료시 threadA 종료
  }
  ```

  </div>

  </div>

</div>

</div>

</div>

<div id="footer" role="contentinfo">

<div class="section footer-body">

Document generated by Confluence on 4월 05, 2026 17:57



</div>

</div>

</div>
